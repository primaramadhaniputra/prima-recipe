import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  container: {
    padding: "2rem 1.3rem",
  },
  heading: {
    marginBottom: "1rem",
  },
  title: {
    marginBottom: "2rem",
  },
});
